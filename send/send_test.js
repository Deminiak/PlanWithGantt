'use strict';

describe('myApp.send module', function() {

  beforeEach(module('myApp.send'));

  describe('send controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});